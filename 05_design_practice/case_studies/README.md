# ケーススタディ

身近なサービスを題材に、01〜04 の各技術を組み合わせて設計・実装する。
各ケースは `design_method` の型（要件確認 → 機能洗い出し → データ設計 → 構成 →
スケール対応 → 運用監視）に沿って README を書き、ドメインの核を実装 + tests で確かめる。

1ケース = 1ディレクトリ。実装はドメインの核に集中し、キャッシュ・分散・レート制限などは
該当レイヤー（01〜04）の実装を参照する形にする。

## ケース一覧

- **url_shortener** — URL短縮サービス。連番＋base62 の採番、解決、クリック計測。
  03 のルータ・01 のキャッシュ・04 のレート制限を実際に組み合わせた動く API まで実装。
- **booking_system**（ロードマップ）— 予約システム。「一番単純な形」から段階的に育てる
  ウォークスルー。stage1（単一リソースの重なり判定）→ stage2（複数リソース・データモデル）→
  stage3（並行制御・API）。希少資源が「時間帯」の典型で、核は処理・並行制御。
- **ec_inventory**（ロードマップ）— EC在庫・注文。stage1（単一商品の在庫）→ stage2（注文・
  明細のデータ設計＝主役）→ stage3（sqlite のトランザクション・制約・API）。希少資源が
  「個数」の典型で、核は**データ設計**。booking と対になる。

（SNSタイムライン・チャットなどは今後追加予定。）
